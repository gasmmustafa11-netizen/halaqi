workflows:
  halaqi-android:
    environment:
      java: 17
    scripts:
      - name: Install dependencies
        script: npm install
      - name: Build Android
        script: |
          cd android
          chmod +x gradlew
          ./gradlew clean assembleDebug
